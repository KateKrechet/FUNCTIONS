#pragma once
#include"stdafx.h"
void PrintRand(int arr[], const int n);
void PrintRand(short arr[], const int n);
void PrintRand(float arr[], const int n);
void PrintRand(double arr[], const int n);
void PrintRand(char arr[], const int n);
void PrintRand(short arr[ROWS][COLS], const int ROWS, const int COLS);
void PrintRand(int arr[ROWS][COLS], const int ROWS, const int COLS);
void PrintRand(float arr[ROWS][COLS], const int ROWS, const int COLS);
void PrintRand(double arr[ROWS][COLS], const int ROWS, const int COLS);
void PrintRand(char arr[ROWS][COLS], const int ROWS, const int COLS);